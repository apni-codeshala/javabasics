/*
 * StringBuilder
 * 
 * StringBuilder is same as the StringBuffer, that is it stores the object in heap and it can be modified
 * The main difference between the StringBuffer and StringBuilder is that StringBuilder is also not thread safe
 * StringBuilder is fast as it is not thread safe
 * StringBuilder method is same as StringBuffered method but not any method is synchronized
 * 
 */

public class StringBuilder {
    public static void main(String[] args) {
        
    }
}
